{
  "skill_class": "ClassificationSkill",
  "system_prompt": "We want to classify short movie reviews by sentiment.",
  "function_definition": {
    "type": "function",
    "function": {
      "name": "categorize_text",
      "description": "Classify text into exactly 1 category out of ['positive', 'negative'].",
      "strict": true,
      "parameters": {
        "type": "object",
        "properties": {
          "categories": {
            "type": "string",
            "enum": [
              "positive",
              "negative"
            ],
            "description": "A chosen category from the provided set."
          }
        },
        "required": [
          "categories"
        ],
        "additionalProperties": false
      }
    }
  }
}